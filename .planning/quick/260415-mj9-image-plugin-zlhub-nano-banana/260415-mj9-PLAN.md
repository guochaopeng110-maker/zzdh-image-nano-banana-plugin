---
quick_task: 260415-mj9
type: execute
autonomous: true
files_modified:
  - image_plugin_zlhub_nano_banana/main.py
  - image_plugin_zlhub_nano_banana/.gitignore
---

<objective>
为 `image_plugin_zlhub_nano_banana` 增加可落地到文件的详细调试日志，覆盖插件启动、参数获取、请求/响应关键内容与生成结果，便于按时间线排查问题。

Purpose: 让调试不再只依赖控制台和内存缓冲，保留可追溯的时间序列日志。
Output: 启用文件日志的插件运行代码（含敏感字段脱敏）与日志文件忽略规则。
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@image_plugin_zlhub_nano_banana/main.py
@image_plugin_zlhub_nano_banana/.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立按时间滚动的文件日志基础设施</name>
  <files>image_plugin_zlhub_nano_banana/main.py, image_plugin_zlhub_nano_banana/.gitignore</files>
  <action>在 `main.py` 的日志初始化区域为 `_setup_logging()` 增加文件日志 handler：日志写入 `image_plugin_zlhub_nano_banana/logs/` 目录，文件名包含日期（例如 `plugin_YYYYMMDD.log` 或按天滚动策略），并在日志格式中包含完整时间（年月日时分秒）与日志级别。保留现有 console + `_BufferingHandler` 行为，不替换。将日志目录与日志文件模式加入 `.gitignore`（如 `logs/` 或 `logs/*.log`），避免调试产物入库。</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('image_plugin_zlhub_nano_banana/main.py').read_text(encoding='utf-8'); gi=Path('image_plugin_zlhub_nano_banana/.gitignore').read_text(encoding='utf-8'); assert 'FileHandler' in t or 'TimedRotatingFileHandler' in t or 'RotatingFileHandler' in t; assert 'logs' in gi.lower()"</automated>
  </verify>
  <done>插件日志同时输出到控制台、内存缓冲和本地文件；日志文件按时间维度可区分且不会被 git 跟踪。</done>
</task>

<task type="auto">
  <name>Task 2: 记录启动、参数与请求/响应详情（含脱敏）</name>
  <files>image_plugin_zlhub_nano_banana/main.py</files>
  <action>在 `main.py` 中补充结构化调试日志点：1) 插件初始化完成时记录基础运行信息与已加载参数；2) `get_params()` 或参数归并路径记录关键参数快照；3) 生成链路中记录请求参数（prompt 摘要、模型、endpoint、timeout、参考图数量等）和响应摘要（HTTP 状态、返回字段、图片 URL/base64 是否存在、错误体片段）。所有日志必须对敏感字段脱敏（如 API Key 仅记录是否设置、长度或前后掩码），不得明文写入 token/key。异常分支补充 traceback 与关联上下文 ID，确保一次任务可串联完整时间线。</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('image_plugin_zlhub_nano_banana/main.py').read_text(encoding='utf-8'); assert '插件初始化完成' in t; assert ('send_doubao_request' in t and ('响应' in t or 'response' in t.lower())); assert ('API Key' in t and ('已设置' in t or '***' in t or '脱敏' in t))"</automated>
  </verify>
  <done>一次生成任务可在日志文件中看到从启动/参数到请求/响应再到落盘结果的完整链路，且敏感信息不会明文泄露。</done>
</task>

<task type="auto">
  <name>Task 3: 用自动化命令验证日志改造未破坏主流程</name>
  <files>image_plugin_zlhub_nano_banana/main.py</files>
  <action>执行快速静态与可导入验证，确保新增日志代码不会造成语法错误或初始化失败。若日志目录在运行时创建，确保目录创建逻辑在不存在目录时也可正常执行（不依赖手动预建目录）。</action>
  <verify>
    <automated>python -m py_compile image_plugin_zlhub_nano_banana/main.py</automated>
  </verify>
  <done>代码可编译通过，插件可继续被宿主加载，日志增强不影响原有生成主链路。</done>
</task>

</tasks>

<success_criteria>
1. 插件运行后会在 `image_plugin_zlhub_nano_banana/logs/` 产生按时间可分析的日志文件。
2. 日志覆盖插件启动、参数读取、请求参数、响应摘要、异常信息与输出结果。
3. API Key 等敏感字段在日志中被脱敏，不出现明文凭据。
4. `python -m py_compile image_plugin_zlhub_nano_banana/main.py` 通过。
</success_criteria>
